HOW TO LOAD DATA FROM U R LOCAL STORAGE:
+++++++++++++++++++++++++++++++++++++++


CSV File Creation Using Notepad

Step 1: Open Notepad
CREATE TABLE 

CREATE OR REPLACE TABLE EMP
(
EID NUMBER,
ENAME VARCHAR,
SAL NUMBER
);



Step 2: OPEN NOTEPAD AND PASTE THE DATA

101,ALLEN,2000
102,SMITH,3000
103,BLAKE,4000
104,JONES,5000
105,KING,6000

⚠️ Values must be separated by commas (,).

Step 3: Save File

Click:

File
 ↓
Save As

File name:

emp.csv



✔ Snowflake UI – Local File Load Steps 
🔵 Step 1: Go to Home
Click → Home (Top left Snowflake logo)
🔵 Step 2: UPLOAD LOCAL FILE
🔵 Step 3:CLICK BROWSE
🔵 Step 4: SELECT CSV FILE
SELECT ------>Database → Schema → Tables → EMP
🔵 Step 5: Click Load Data
EMP page → Load Data button


CHECK-----SELECT * FROM EMP;

DDL:
++++
1)CREATE (ALREDY DONE)
++++++++++++++++++++++

2)RENAME TABLE:
++++++++++++++++
SYNTAX:
ALTER TABLE TN
RENAME TO NEW NAME ;

EX:

ALTER TABLE EMP
RENAME TO EMPLOYEES;

SELECT * FROM EMPLOYEES;
3)ALTER:
++++++++
I)TO ADD COLUMN:
---------------
SYNTAX:

ALTER TABLE TN
ADD COL_NAME DATA TYPE  [CONS];

EX:

ALTER TABLE EMPLOYEES
ADD CNO NUMBER(10) UNIQUE;

II)TO RENAME COLUMN:
--------------------
SYNTAX:

ALTER TABLE TN
RENAME COLUMN COL_NAME TO NEW_NAME;

ALTER TABLE EMPLOYEES
RENAME COLUMN CNO TO PHNO;

TRUNCATE TABLE EMPLOYEES;


SELECT * 
FROM EMPLOYEES;

INSERT INTO EMPLOYEES
SELECT *
FROM EMPLOYEES AT(OFFSET=>-600);


DROP TABLE EMPLOYEES;

UNDROP TABLE EMPLOYEES;


DESC TABLE EMPLOYEES;

SELECT * FROM EMPLOYEES;

ALTER TABLE old_table_name
RENAME TO new_table_name;


ALTER TABLE EMPLOYEES
DROP COLUMN PHNO;

SELECT *
FROM EMPLOYEES;


TRUNCATE TABLE EMP;

insert into emp
SELECT *
FROM EMP AT(OFFSET=>-30);



UPDATE EMPLOYEES
SET SAL=10000
WHERE EID=101;

DELETE FROM EMPLOYEES WHERE EID=106;















